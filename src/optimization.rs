use lut::Pipeline;
use transform::TransformFlags;
use {Intent, PixelFormat};

pub fn optimize_pipeline(
    _: &Pipeline,
    _: Intent,
    _: PixelFormat,
    _: PixelFormat,
    _: TransformFlags,
) {
    // TODO
    // unimplemented!()
}
